name: PR Review with Progress Tracking

# This example demonstrates how to use the track_progress feature to get
# visual progress tracking for PR reviews, similar to v0.x agent mode.

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  review-with-tracking:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: PR Review with Progress Tracking
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

          # Enable progress tracking
          track_progress: true

          # Your custom review instructions
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            ═══════════════════════════════════════════════════════════════════════════════
            ⚠️  CRITICAL: DECISION TABLE IS MANDATORY - MUST BE FIRST COMMENT ON PR
            ═══════════════════════════════════════════════════════════════════════════════

            Before doing ANYTHING else, you MUST:
            1. Review the PR changes
            2. Evaluate all criteria below
            3. Post the DECISION TABLE as the FIRST PR comment using: gh pr comment ${{ github.event.pull_request.number }} --body "..."

            **THE DECISION TABLE MUST BE POSTED BEFORE ANY OTHER COMMENTS OR INLINE REVIEWS.**

            ---

            ## DECISION TABLE FORMAT (REQUIRED - POST THIS FIRST)

            **Decision Rules:**
            - 🟢 APPROVE MERGE: 0 critical issues AND 0 high issues
            - 🔴 REQUEST CHANGES: Any critical OR high issues found

            Post this EXACT markdown structure as your FIRST comment:

            ## 🔍 PR Review Decision

            | Decision | |
            |----------|---------|
            | **Status** | 🟢 **APPROVE MERGE** _or_ 🔴 **REQUEST CHANGES** |

            ---

            ### Criteria Met

            | Criterion | Status | Details |
            |-----------|--------|---------|
            | **Critical Issues** | ✅ PASS / ❌ FAIL | X critical issues |
            | **High Issues** | ✅ PASS / ❌ FAIL | X high issues |
            | **Medium Issues** | ✅ PASS / ⚠️ WARN | X medium issues - non-blocking |
            | **Security (OWASP)** | ✅ PASS / ❌ FAIL | Summary of security checks |
            | **Architecture (SOLID)** | ✅ PASS / ❌ FAIL | Design pattern assessment |
            | **Test Coverage** | ✅ PASS / ❌ FAIL | Test coverage assessment |
            | **Type Safety** | ✅ PASS / ❌ FAIL | Type validation status |

            ---

            ### Summary
            Brief summary of the review findings and any blocking issues.

            ---

            **Status Logic:**
            - Critical Issues: PASS if 0, FAIL if > 0
            - High Issues: PASS if 0, FAIL if > 0
            - Medium Issues: PASS if 0, WARN if > 0 (non-blocking)
            - Security (OWASP): PASS if no OWASP Top 10 vulnerabilities found
            - Architecture (SOLID): PASS if code follows SOLID principles
            - Test Coverage: PASS if adequate tests exist for changes
            - Type Safety: PASS if proper type hints/validation (Pydantic for Python, TypeScript types, etc.)

            ═══════════════════════════════════════════════════════════════════════════════
            AFTER posting the decision table, perform detailed review with these focus areas:
            ═══════════════════════════════════════════════════════════════════════════════

            1. **Code Quality**
               - Clean code principles and best practices
               - Proper error handling and edge cases
               - Code readability and maintainability

            2. **Security**
               - Check for potential security vulnerabilities
               - Validate input sanitization
               - Review authentication/authorization logic

            3. **Performance**
               - Identify potential performance bottlenecks
               - Review database queries for efficiency
               - Check for memory leaks or resource issues

            4. **Testing**
               - Verify adequate test coverage
               - Review test quality and edge cases
               - Check for missing test scenarios

            5. **Documentation**
               - Ensure code is properly documented
               - Verify README updates for new features
               - Check API documentation accuracy

            Provide detailed feedback using inline comments for specific issues.
            Use top-level comments for general observations or praise.

          # Tools for comprehensive PR review
          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"

# When track_progress is enabled:
# - Creates a tracking comment with progress checkboxes
# - Includes all PR context (comments, attachments, images)
# - Updates progress as the review proceeds
# - Marks as completed when done