name: PR Review with Progress Tracking

# This example demonstrates how to use the track_progress feature to get
# visual progress tracking for PR reviews, similar to v0.x agent mode.

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

jobs:
  review-with-tracking:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: PR Review with Progress Tracking
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

          # Enable progress tracking
          track_progress: true

          # Your custom review instructions
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            ═══════════════════════════════════════════════════════════════════════════════
            ⚠️  CRITICAL: DECISION TABLE IS MANDATORY - MUST BE FIRST COMMENT ON PR
            ═══════════════════════════════════════════════════════════════════════════════

            Before doing ANYTHING else, you MUST:
            1. Review the PR changes
            2. Evaluate all criteria below
            3. Post the DECISION TABLE as the FIRST PR comment using: gh pr comment ${{ github.event.pull_request.number }} --body "..."

            **THE DECISION TABLE MUST BE POSTED BEFORE ANY OTHER COMMENTS OR INLINE REVIEWS.**

            ---

            ## DECISION TABLE FORMAT (REQUIRED - POST THIS FIRST)

            **Decision Rules:**
            - 🟢 APPROVE MERGE: 0 critical AND 0 high issues
            - 🟡 MERGE WITH CAUTION: 0 critical, 0 high, has medium/low
            - 🔴 DO NOT MERGE: Any critical OR high issues found

            Post this EXACT markdown structure as your FIRST comment:

            ## 🔍 PR Review Decision

            | Decision | |
            |----------|---------|
            | **Status** | 🟢 **APPROVE MERGE** / 🟡 **MERGE WITH CAUTION** / 🔴 **DO NOT MERGE** |

            ---

            ### Criteria Met

            | Criterion | Status | Details |
            |-----------|--------|---------|
            | 🔴 **Critical Issues** | ✅ PASS / ❌ FAIL | X critical issues |
            | 🟠 **High Issues** | ✅ PASS / ❌ FAIL | X high issues |
            | 🟡 **Medium Issues** | ✅ PASS / ⚠️ WARN | X medium issues - non-blocking |
            | 🔒 **OWASP Top 10** | ✅ PASS / ❌ FAIL | A01-A10 compliance |
            | 📐 **SOLID Principles** | ✅ PASS / ❌ FAIL | SRP, OCP, LSP, ISP, DIP |
            | 🔁 **Code Reuse (DRY)** | ✅ PASS / ❌ FAIL | Duplication check |
            | 🧪 **Test Coverage** | ✅ PASS / ❌ FAIL | Coverage assessment |
            | 📝 **Type Safety** | ✅ PASS / ❌ FAIL | Type hints/Pydantic |
            | ⚡ **Performance** | ✅ PASS / ❌ FAIL | N+1 queries, bottlenecks |

            ---

            ### Summary
            Brief summary of the review findings and any blocking issues.

            ---

            **Status Logic:**
            - Critical/High Issues: PASS if 0, FAIL if > 0
            - Medium Issues: PASS if 0, WARN if > 0 (non-blocking)
            - OWASP Top 10: A01-Broken Access, A02-Crypto, A03-Injection, A04-Insecure Design, A05-Misconfig, A06-Vulnerable Components, A07-Auth Failures, A08-Integrity, A09-Logging, A10-SSRF
            - SOLID: SRP, OCP, LSP, ISP, DIP compliance
            - DRY: No duplicate code, reuse existing utilities
            - Performance: No N+1 queries, proper caching, async patterns

            ═══════════════════════════════════════════════════════════════════════════════
            AFTER posting the decision table, perform 3-LOOP REVIEW:
            ═══════════════════════════════════════════════════════════════════════════════

            **Loop 1 - Security & Architecture:**
            - OWASP Top 10 (SQL injection, XSS, auth bypass, SSRF)
            - SOLID principles violations
            - Authorization checks on endpoints
            - Hardcoded secrets, crypto failures

            **Loop 2 - Implementation:**
            - Clean code, error handling, input validation
            - Code reuse (DRY), design patterns
            - Async/await patterns, proper exception handling

            **Loop 3 - Polish:**
            - Performance (N+1 queries, caching, indexing)
            - Test coverage, edge cases
            - Type hints, Pydantic validation
            - Documentation completeness

            Provide inline comments for specific issues. Decision table is MANDATORY for every review.

          # Tools for comprehensive PR review
          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"

# When track_progress is enabled:
# - Creates a tracking comment with progress checkboxes
# - Includes all PR context (comments, attachments, images)
# - Updates progress as the review proceeds
# - Marks as completed when done